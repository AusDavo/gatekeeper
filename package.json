{
  "name": "gatekeeper",
  "version": "1.0.0",
  "description": "Bitcoin multisig wallet signature verification tool",
  "scripts": {
    "build": "browserify main.js -o bundled.js",
    "format": "prettier --write ."
  },
  "dependencies": {
    "bitcoinjs-lib": "^4.0.5",
    "bitcoinjs-message": "^2.0.0",
    "bs58check": "^3.0.1",
    "secp256k1": "^5.0.0"
  },
  "devDependencies": {
    "browserify": "^17.0.0",
    "prettier": "^3.1.1"
  }
}
